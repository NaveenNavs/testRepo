spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: root
  jpa:
   hibernate:
    ddl-auto: update
  zipkin:
    base-url: http://127.0.0.1:9411/
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ccc9e766ea9f23437615
            client-secret: c300e9b084c416b2dfec0836065b1468b6b20024
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
management:
  endpoints:
    web:
      exposure:
        include:
        - "*"
springdoc:
  swagger-ui:
    path: coupon-swagger-ui